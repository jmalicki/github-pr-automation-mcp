{
  "name": "resolve-pr-mcp",
  "version": "0.1.0",
  "description": "MCP server for automated GitHub PR resolution and management",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "resolve-pr-mcp": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc && node dist/index.js",
    "cli": "tsc && node dist/cli.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "github",
    "pull-request",
    "automation",
    "ci"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@octokit/rest": "^20.0.2",
    "@octokit/auth-app": "^6.0.3",
    "commander": "^11.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

